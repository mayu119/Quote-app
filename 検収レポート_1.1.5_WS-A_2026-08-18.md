# 検収レポート 1.1.5 WS-A

検収日: 2026-08-18  
対象: リテンション計測の検収と完遂（WS-A）

## 結論

A-0の8項目はコードレビュー上すべてOK。A-1も実装完了し、ペイウォール表示計測はPremiumViewへ集約した。

ただし、指定のxcodebuildは環境側のCoreSimulatorService停止とSPM product解決不全により、コンパイル開始前に終了した。変更対象Swiftのparse-only検証は成功している。

## A-0 検収結果

| 項目 | 結果 | 確認内容 |
|---|---|---|
| 1. 通知開封計測 | OK | `QuoteApp.swift`で`UNUserNotificationCenterDelegate`を設定し、通知タップ時に`notification_opened`を送信。通知IDから`notification_type`を復元し、`copy_mode`がある通知は`variant`も付与している。 |
| 2. 通知スケジュール計測 | OK | `NotificationService.swift`の無料1件・プレミアム複数件の登録成功後に`notification_schedule`を送信。`is_premium`、`time_slot_count`、teaseのvariantも整合している。 |
| 3. 通知コピーのtease統一 | OK | `NotificationCopyMode`は`.tease`のみ。本文はtease固定で、旧`full`分岐と端末ごとのA/B割当（CryptoKitを含む）は除去されている。 |
| 4. オンボ直後ペイウォール計測 | OK | オンボーディングの処方箋リビール後に`PremiumView(context: .onboarding)`が表示され、A-1で全コンテキスト共通の計測へ拡張した。 |
| 5. 夜間表示計測 | OK | `DailyDrawView`の表示時に`isNight == true`なら`night_word_view`を送信する。既存の`NightWordView`経路も維持されている。 |
| 6. 通知許可タイミング移動 | OK | Step 5直後の許可要求はなく、処方箋の「続ける」経路でのみ未決定状態のOS許可要求を行う。許可後は通知をスケジュールする。 |
| 7. 明日の一枚の予告カード | OK | `OnboardingReturnPromiseView`を処方箋スキップ後とペイウォール離脱後に表示し、表示時に`onboarding_return_promise_view`を送信する。 |
| 8. onboarding_complete集約・重複ガード | OK | `finishOnboarding()`に`didLogOnboardingComplete`ガードを置き、ジャンル選択・設定更新・`onboarding_complete`・初回起動完了を同経路に集約している。 |

## A-1 実装内容

- `PremiumView.onAppear`で`context.rawValue`をtriggerに`paywall_view`を送信するよう集約した。View再表示による同一インスタンス内の重複も`didLogPaywallView`で抑止している。
- `CategoryPickerView`の2箇所、`ContentView`の4箇所にあった表示元の`logPaywallView`を削除した。
- `free_preview_locked`と`home_toolbar`は`PaywallContext`に専用コンテキストを追加し、従来の文脈差を保持した。
- CategoryPickerのロック経路は`.categoryLock`、壁紙は`.wallpaper`、お気に入り上限は`.favoriteLimit`、ストリーク救済は`.streakRescue`を渡すようにした。
- FavoritesViewの解説シート経由を`.favoriteLimit`から`.insight`へ修正した。
- SettingsView、ArchiveView、FavoritesView、DailyCheckInViewの表示経路もPremiumView経由で自動的に計測されることを確認した。`logPaywallView`の呼び出しはAnalyticsServiceの定義とPremiumViewの集約箇所だけになっている。
- `purchase_initiate`は既存どおり`context.rawValue`を使用しているため、`paywall_view.trigger`と同じ語彙になる。

## 発見した問題と対処

1. 先行差分では`paywall_view`がオンボーディング時だけPremiumView内にあり、それ以外は表示元から送信されていた。集約後に二重送信が起きる状態だったため、PremiumViewへ一本化して表示元の送信を削除した。
2. FavoritesViewの解説シートが`.favoriteLimit`を使っており、解説導線のtriggerが誤っていた。`.insight`へ修正した。
3. CategoryPickerの表示元送信を削除すると、コールバック先が`.general`のままではカテゴリロックの文脈が失われるため、`.categoryLock`へ修正した。
4. 指定ビルドでは、CoreSimulatorServiceが`Connection refused`となり、利用可能なSimulator runtimeを検出できなかった。またSPM product（`RevenueCat`、`FirebaseCore`、`FirebaseAnalytics`）がPIFに取り込まれず、`Missing package product`で停止した。エラーは変更ファイルのコンパイル前に発生しており、自分のSwift変更起因とは判定できない。

## 残る懸念

- `UNUserNotificationCenterDelegate`の`willPresent`は未実装。アプリ前景中に通知が届いても、OSの通知表示は抑制されるため、その状態でユーザーが通知をタップして`notification_opened`を発火する経路はない。バックグラウンド・ロック画面からのタップ計測には影響しない。前景表示を要件化する場合は別途`willPresent`の方針決定が必要。
- 処方箋をスキップした場合、通知権限が`.notDetermined`ならOS許可を一度も聞かず、`notificationGranted`はfalse、通知も無効化される。処方箋を見せたユーザーにだけ許可を求める設計は体験上は妥当だが、スキップユーザーのD1通知リーチを失う。Settingsから後で再有効化できるため、今回は仕様上の残課題として扱う。
- `notification_opened`の`copyVariant`は`copy_mode`を持つ日次・プレミアム通知では付与されるが、実験対象外の週次棚通知には付与されない。通知コピー実験の対象範囲としては整合している。

## ビルド結果

スキーム確認:

- コマンド: `xcodebuild -list -clonedSourcePackagesDirPath ./build/SPM -packageCachePath ./build/PackageCache`
- 結果: 成功
- スキーム: `QuoteApp`、`com.antigravity.QuoteAppExtension`

ビルド:

- コマンド: `xcodebuild -scheme QuoteApp -configuration Debug -destination 'generic/platform=iOS Simulator' -derivedDataPath ./build/DerivedData -clonedSourcePackagesDirPath ./build/SPM -packageCachePath ./build/PackageCache build`
- 結果: 失敗（exit code 65）
- 停止理由: `CoreSimulatorService connection became invalid`／`Supported platforms ... is empty`の後、`Missing package product 'RevenueCat'`、`'FirebaseCore'`、`'FirebaseAnalytics'`
- 補助確認: 変更対象Swift 9ファイルの`swiftc -parse`は成功。実行確認・Simulator起動は行っていない。

### 追記(Claude、2026-08-18 01:44)

上記ビルド失敗はLunaのサンドボックス環境起因(CoreSimulatorService接続不可)であることを確認。サンドボックス外で同等のビルドを再実行し、**BUILD SUCCEEDED** を確認済み:

- コマンド: `xcodebuild -project QuoteApp.xcodeproj -scheme QuoteApp -configuration Debug -destination 'generic/platform=iOS Simulator' -derivedDataPath ./build/DerivedData build`
- 結果: 成功(exit 0)

また、A-1のtrigger語彙にcamelCase(`favoriteLimit`等、既存rawValue由来)とsnake_case(`free_preview_locked`等、旧trigger文字列温存)が混在する。`purchase_initiate`と`paywall_view`の間では完全一致しており実害はないが、ダッシュボード整備時に表記揺れとして見える点を記録しておく。

## 制約遵守

- `git commit`は実行していない。
- `QuoteApp/Sources/Resources/`には書き込んでいない。`quotes.json`にも接触していない。
- ペイウォール文言・価格・プラン構成は変更していない。
- 既存の先行差分は破棄・リバートしていない。
