# リリース要件定義 1.1.5 — リテンション計測×コンテンツ再設計 (2026-08-18)

> 経緯: 課金ゼロ分析(`課金ゼロ分析_GA4×競合_2026-08-17.md`)とリテンション診断(P-579コメント)を受け、Mayuが2026-08-18に実行を承認。2ワークストリームをCodex Luna並列で実行する。
> 前提: リテンション修理(WS-A)の実装は2026-08-17 18:24-18:29に未コミット差分として**既に大半が存在する**(実装者の記録なし)。WS-Aは新規実装ではなく「全行検収+残課題完遂+ビルド検証」とする。
> フェーズ規律: ペイウォールの訴求文言・価格・プラン構成には**一切触れない**(P-578フェーズ2の領域)。今回は計測・リテンション・コンテンツのみ。

---

## WS-A: リテンション計測の検収と完遂

### A-0. 検収対象(既存の未コミット差分・6ファイル)

対象: `QuoteApp.swift` / `AnalyticsService.swift` / `NotificationService.swift` / `DailyDrawView.swift` / `OnboardingView.swift` / `PremiumView.swift`

実装済みとされる内容(全行レビューして正しさを確認する。**リバート禁止**):
1. 通知開封計測: AppDelegateにUNUserNotificationCenterDelegate配線、`notification_opened`送信(copyVariant付き)
2. `notification_schedule`送信(無料/プレミアム両経路)
3. 通知コピーのtease型統一(full廃止、A/B割当コード削除)
4. オンボ直後ペイウォールの`paywall_view`送信
5. `night_word_view`送信(DailyDrawView夜間表示時)
6. OS通知許可プロンプトをオンボStep5から処方箋リビール直後へ移動
7. 「明日の一枚は、もう伏せてあります。」の予告カード(OnboardingReturnPromiseView + `onboarding_return_promise_view`)
8. `onboarding_complete`のfinishOnboarding集約+重複ガード

検収観点: ロジックの正誤、イベントパラメータの整合、リグレッション(通知が既存ユーザーで止まらないか、オンボが全パスで完走するか)、フォアグラウンド受信時の表示(willPresent未実装なら影響を評価)。

### A-1. 残課題の実装

1. **paywall_view計測の集約と語彙統一**(P-579案4の完遂):
   - `PremiumView.onAppear`で**全コンテキスト**の`paywall_view`を送信(trigger = `context.rawValue`)に集約する
   - 既存の呼び出し側`logPaywallView`(CategoryPickerView 2箇所、ContentView 4箇所)は削除し、二重送信を防ぐ。ただし呼び出し側triggerの詳細度が失われる場合(`free_preview_locked`と`home_toolbar`の区別等)は、PaywallContextに不足コンテキストを追加して区別を維持する
   - これにより`paywall_view.trigger`と`purchase_initiate.trigger`(既にcontext.rawValue)の語彙が一致する
   - FavoritesViewの解説シート経由が`.favoriteLimit`に誤マッピングされている問題を修正し、未使用の`.insight`コンテキストを充てる
2. 上記に伴い、未計測だった表示元(SettingsView / ArchiveView / FavoritesView / DailyCheckInView)も自動的に計測対象になることを確認する

### A-2. 受け入れ基準

- [ ] `xcodebuild`でビルド成功(derivedDataPathはリポジトリ内`./build/DerivedData`を使用)
- [ ] 全ペイウォール表示経路で`paywall_view`が1回だけ送信される(コードレビューで確認)
- [ ] `paywall_view.trigger`と`purchase_initiate.trigger`が同一語彙
- [ ] オンボの全分岐(処方箋→続ける/スキップ、ペイウォール→購入/後で)でクラッシュ・詰みがないことをコードレビューで確認
- [ ] 検収レポート提出: 検収結果、発見した問題と修正内容、残る懸念(例: 処方箋スキップ経路では通知許可を聞かない設計の妥当性)を記載
- [ ] コミットはしない(Mayuレビュー後)

### A-3. 禁止事項

- ペイウォール訴求文言・価格・プラン構成の変更
- スコープ外のリファクタ・整形
- `quotes.json`への接触(WS-Bが並行作業中)
- 既存未コミット差分の破棄・リバート

---

## WS-B: コンテンツ再設計(P-561の実行)

### B-0. 背景と目的

quotes.jsonは300件・10カテゴリ×30件。うち167件(56%)が`author: "Original"`で、これは設計判断を経ない水増し(ピボット時は10%だった)。実在の名言が主軸のアプリとして成立する構成に再設計する。**訴求主軸「棚」・ターゲット(夜に自分を責める28歳前後の女性)・免罪/慰めトーンは維持**。

現状のカテゴリ別Original件数: affirmation 30/30、want_to_quit 24、love_crush 19、self_love 16、for_my_child 15、inner_strength 14、courage 13、family_love 13、relationships 12、positive 11。

### B-1. 構成要件

- 総数300件・10カテゴリ×30件・既存スキーマ(16フィールド)を完全維持。アプリコードには一切触れない
- **affirmationカテゴリ(30件)**: 一人称アファメーションはOriginalが設計上の役割。現状維持(文言の微修正は可)
- **非affirmation 9カテゴリ**: 各カテゴリ**実在名言24件以上(Original 6件以下)**。残すOriginalは既存137件から「行動描写が具体的で強いもの」を選抜し、選定理由をレポートに記す
- 実在名言の著者は**7割以上を女性**とする(既存路線: Brené Brown、オードリー・ヘプバーン、ミシェル・オバマ、黒柳徹子等)。日本人著者も各カテゴリに1件以上入れる
- want_to_quitのような直接的名言が枯れやすいカテゴリは、テーマの近接(休むこと・境界線・燃え尽き・完璧主義からの離脱)で実在名言を充てる

### B-2. 品質・検証要件(最重要)

1. **出典検証必須**: 採用する実在名言は原典(書籍名・スピーチ・インタビュー・書簡等)を特定できるもののみ。名言まとめサイト単独を根拠にしない。Wikiquote等でmisattributed(誤帰属)とされるものは不採用
2. **検証できない名言は採用しない。数合わせのための創作・改変は絶対禁止**(検証済みが24件に届かないカテゴリは、届かなかった事実と件数をレポートに正直に書く。その分はOriginal選抜で埋めてよい)
3. **翻訳**: 英語圏の名言は`quote_en`に原文、`quote_ja`は自前の新訳(既存の出版訳・ネット訳の流用禁止=翻訳著作権)。訳のトーンは平坦な日常語。説教調・格言調の過剰な装飾禁止
4. **NG語・声のルール**: 動機の名指し禁止(「自己肯定感を上げよう」等)。stop-ai-slop-jp基準(全角ダッシュ、造語メタファー、必殺技語の禁止)を`quote_ja`/`meaning_preview`/`meaning_premium`/`push_notification_hook`全てに適用
5. `meaning_preview`(無料)/`meaning_premium`(PRO)は全件書き下ろし。既存の長さ・語り口(アプリの声=静かで説教しない)に合わせる
6. `id`は既存を維持(残す件)、新規は既存の採番パターンを継続。`background_image`は既存で使用されている値のみ使用。`punchline`は既存慣行に従う(短い名言はquote_jaと同一、長文は要約形)
7. **著作権ルール(2026-08-18追補、WS-B初回成果の監査で判明した欠落)**: 詩・短歌・歌詞の本体引用は、著者の死後70年が経過しパブリックドメインが確定しているもののみ(数行の詩は数行で作品全体であり、引用の主従関係が成立しないため)。存命・保護期間中の著者は、書籍・スピーチ・インタビューの**散文1〜2文の短文引用+出典明記**のみ可(業界慣行水準の中リスクとしてレポートに区分記載)
8. **本文の一意性(同上追補)**: `quote_ja`の重複は300件全体でゼロであること(カテゴリを跨ぐ同一名言の使い回し禁止)。機械検証に本文重複チェックを含める
9. **文脈適合(同上追補)**: 夜のセルフケア文脈に不適合な題材(戦争・死別の悲嘆を直接扱う詩等)は、文言単体が美しくても採用しない

### B-3. 受け入れ基準

- [ ] `quotes.json`更新済み: 300件・各カテゴリ30件・スキーマ完全一致・id重複なし(Pythonでの機械検証結果をレポートに貼る)
- [ ] 非affirmationカテゴリのOriginal合計が54件以下(各カテゴリ6件以下)
- [ ] 検証レポート `コンテンツ再設計_検証レポート_2026-08-18.md` 提出: 全採用名言の出典一覧、カテゴリ別集計(実在/Original/女性比率)、残したOriginalの選定理由、検証できず不採用にした候補リスト
- [ ] コミットはしない(Mayuレビュー後)

### B-4. 進め方の指定

カテゴリ単位で完結させながら進める(1カテゴリ分の検証済み名言→JSON化→次へ)。中断してもカテゴリ単位で成果が残るよう、作業中の候補バンクを作業ファイルに保存しながら進めること。

---

## リリース判定

- WS-A/WS-B両方の成果物をMayuがレビュー→承認後にコミット・ビルド・提出(appstoreconnect-uploadフロー)
- バージョン: 1.1.5 (build 13)
- 本要件の変更はいずれもApp Store審査上の新規リスクなし(コンテンツ差し替え+計測+通知文言)を想定。提出前にprivacy-manifest-audit相当の確認は不要(SDK変更なし)

## 参照

- `課金ゼロ分析_GA4×競合_2026-08-17.md` / P-579(リテンション診断コメント) / P-561(コンテンツ再設計) / `アプリ現状まとめ_2026-08-14.md` §5
