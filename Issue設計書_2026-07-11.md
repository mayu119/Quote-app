# Issue設計書 — Words For Me

作成日: 2026-07-11
最終要件確定日: 2026-07-13
Issue番号はタスク管理の #1〜#25 と一致。
根拠資料: [監査とロードマップ_2026-07-11.md](監査とロードマップ_2026-07-11.md) / [名言データ品質監査_2026-07-11.md](名言データ品質監査_2026-07-11.md) / [今後の展開_世界のマネタイズリサーチと戦略_2026-07-11.md](今後の展開_世界のマネタイズリサーチと戦略_2026-07-11.md)

## v2正式仕様

- 起動体験: [QuoteApp/docs/daily-draw-v2-spec.md](QuoteApp/docs/daily-draw-v2-spec.md)
- ギフト商品・配送: [QuoteApp/docs/gift-omamori-product-spec.md](QuoteApp/docs/gift-omamori-product-spec.md)
- KPI・A/B・SNS運用: [QuoteApp/docs/experiment-operations-v2.md](QuoteApp/docs/experiment-operations-v2.md)
- コピー・ASO・投稿12本: [QuoteApp/docs/copy-strategy-v2.md](QuoteApp/docs/copy-strategy-v2.md)

上記4文書を各領域の一次資料とし、本書と表現が競合する場合は各正式仕様を優先する。2026-07-13時点で、要件上の未決事項はない。未チェックの受け入れ条件は実装・外部設定・実機・期間運用による検証待ちを表す。

見積: S=半日以内 / M=1〜3日 / L=1週間前後

---

# Sprint 0 — リスク除去と土台(今週)

## Issue #1: ペイウォールの架空レビューを差し替え/削除 [S]

**背景**: PremiumView.swift:244-255 に出典のない星5レビュー3件。App Store Guideline(誤解を招く表示)のリジェクトリスク+倫理ライン違反。

**実装設計**:
- `reviewCarouselSection` と `reviewCard()` を削除。呼び出し元(body内 line 77)も除去
- 実レビューがApp Storeに存在する場合のみ、後日「実レビュー引用セクション」として復活(出典: App Store表記を明記、原文ママ)
- 空いた縦スペースには何も足さない(ペイウォールは短いほど良い)

**受け入れ条件**:
- [ ] 架空の人物・評価がアプリ内から完全に消えている
- [ ] ビルドが通り、ペイウォールのスクロールが自然
- [ ] logPaywallView 等の計測が壊れていない

## Issue #2: ペイウォールに恐怖の先回り3点を常設 [S]

**背景**: 「いつでも解約」が月額プラン説明にしか無い。若年女性のサブスク恐怖(勝手に課金される)の中和が転換率に直結。

**実装設計**: `checkoutSection` のCTAボタン直下(「後で見る」の上)に3行の固定表示を追加。

```
文言案(SF Symbol + 12pt):
✓ いつでも解約できます。手順も簡単です
✓ 解約しても、棚に置いた言葉は消えません
✓ 無料期間中に料金はかかりません(終了前に通知)
```

- 3行目はイントロオファー適格時のみ表示(`eligibleIntroductoryDiscount` を流用)
- 「終了前に通知」を書く場合は実際にローカル通知を実装すること(#6と連動。嘘は書かない)

**受け入れ条件**:
- [ ] 3点がプラン選択と同一ビューポート内に見える
- [ ] トライアル非適格ユーザーには3行目が出ない
- [ ] 文言が世界観の声(です・ます、静かなトーン)で書かれている

## Issue #3: DesignSystem.swift 新設(トークン集約) [M]

**背景**: ハードコード色247件/固定フォント334件/画面ごとに微妙に違うピンクベージュ3種。ドリフトとDynamic Type全滅の根本原因。

**実装設計**: `Sources/DesignSystem.swift` を新規作成。**既存の見た目を変えないことが原則**(値の付け替えのみ)。

```swift
enum WFM {  // Words For Me
    enum ColorToken {
        // Asset Catalog "WFM/..." に light/dark ペアで定義し参照
        static let pageBase = Color("WFM/PageBase")        // 現 F1E9E3 系を正とする
        static let sheetBase = Color("WFM/SheetBase")      // FFFCF8
        static let textPrimary = Color("WFM/TextPrimary")  // 2F2730
        static let textSub = Color("WFM/TextSub")          // 7D7280
        static let accentRose = Color("WFM/AccentRose")    // EAA3A1
        static let accentGold = Color("WFM/AccentGold")    // PremiumViewの0.86,0.55,0.60を統合
        // 夜系(メイン画面)は意図的な固定ダーク: NightVeil, NightScrim...
    }
    enum Radius { static let s: CGFloat = 12; static let m: CGFloat = 18; static let l: CGFloat = 26; static let xl: CGFloat = 34 }
    enum Space  { static let xxs: CGFloat = 4; static let xs: CGFloat = 8; static let s: CGFloat = 12; static let m: CGFloat = 16; static let l: CGFloat = 24; static let xl: CGFloat = 32 }
    enum Type {  // relativeTo 付きでDynamic Type対応
        static func title() -> Font { .system(.title2, weight: .bold) }
        static func body() -> Font { .system(.body) }
        // カスタムサイズが必要な箇所: .custom(name, size:, relativeTo:)
    }
    enum Motion { static let quick = Animation.spring(response: 0.34, dampingFraction: 0.82); static let smooth = Animation.spring(response: 0.48, dampingFraction: 0.9) }
}
```

- 各Viewのprivate色定義との対応表をコメントで残す(どの旧値をどのトークンに寄せたか)
- 置換作業自体は #17 で画面単位に実施。本Issueはトークン定義+Asset Catalog登録まで

**受け入れ条件**:
- [ ] 3画面で異なっていたピンクベージュが1つの正に統一定義されている
- [ ] ライト/ダーク両方の値がAsset Catalogにある(メイン画面の意図的ダークはコメントで明示)
- [ ] 新規実装はこのトークンのみで書ける状態(色・角丸・余白・フォント・モーション)

## Issue #4: 未使用Config削除と価格表示の整合確認 [S]

**実装設計**:
- `Config.freeUserDailySwipeLimit = 50` を削除(全ソース未参照を確認済み)
- `Config.monthlyPrice/yearlyPrice`(¥600/¥4,800)の表示用定数が実際に使われている箇所を洗い出し、RevenueCatの `localizedPriceString` に一本化できる箇所は一本化(ASCで価格改定した時にズレる事故の防止)
- ペイウォール比較表の各行(通知3回/Live Activity/壁紙/アーカイブ)が実装の実態と一致しているか照合

**受け入れ条件**:
- [ ] ハードコード価格文字列が表示経路から消えるか、ASC価格と一致することがテストで担保
- [ ] 比較表の全行が実機能と一致

## Issue #18: 名言データ不良12件の即時修正 [S]

**背景**: 監査A節。アプリが読むのは quotes.json(`Bundle.main.url(forResource: "quotes")`)。

**実装設計**(quotes.json と quotes_full.json 両方に適用):
1. `women_quote_183`: author_kana「イチロー」→「ヴィクトール・フランクル」。quote_en を quote_ja に一致する原文(*"the pursuit of meaning"系*)へ差し替え、無ければ quote_ja を quote_en の訳に修正
2. 完全重複の削除: `women_quote_044`(011と同一)、`women_quote_087`(047と同一)、`women_quote_184`(182と同一原文)→ 3枠は #22 のOriginalで補充するまで一時的に19件でも可(コード上カテゴリ件数のハード依存が無いことを確認する)
3. EN/JA不一致の修正: 100, 102, 109, 129 → quote_en を正とし quote_ja を正しい訳に差し替え(またはペア自体を削除)
4. `QuoteDataService.currentDataVersion` をインクリメントし、お気に入り保護つきフルリフレッシュを発火

**受け入れ条件**:
- [ ] `python3 スクリプト`で再検証: kana不一致0、同一カテゴリ内EN重複0、EN/JA不一致0
- [ ] 既存ユーザーのお気に入りが移行後も保持される(削除IDをお気に入り済みの場合の挙動を定義: 棚には残す)
- [ ] quotes.json と quotes_full.json の差分が意図したもののみ

## Issue #19: 権利リスク著者の判断と対処 [S — 判断が主]

**背景**: 監査C節。

**実装設計**:
1. **削除(推奨確定)**: 相田みつを2件(049, 178)、吉野源三郎「君たちはどう生きるか」(098) — 書名であり名言でない
2. **ユーザー判断が必要**(このIssueで決める): 存命人物6件(黒柳徹子3・イチロー2・大谷翔平1)を残すか。残す場合は「発言事実の紹介」の体裁を守る(author_factの充実、改変しない)
3. 没後間もない著者(稲盛和夫・瀬戸内寂聴・佐々木正美・手塚治虫・松下幸之助・本田宗一郎)は優先度中。#22の増強が進んだ段階で段階的にOriginal置換
4. 削除枠は #22 で補充

**受け入れ条件**:
- [ ] 相田みつを・書名の3件がデータから消えている
- [ ] 存命人物の掲載方針が文書化されている(このファイルに追記)

### 決定記録 — 2026-07-11

- **存命人物は残す**: 黒柳徹子(3件)、イチロー(2件)、大谷翔平(1件)は、本人の発言事実を紹介する引用として掲載を継続する。
- **運用基準**: 今後の追加・更新では、発言の改変を行わず、一次資料または信頼できる掲載元を `source_context` に記録する。出典を確認できない候補は掲載しない。
- **今回の反映**: 権利リスクとして削除を確定していた相田みつを2件(`women_quote_049`、`women_quote_178`)と書名1件(`women_quote_098`)は、両データファイルから削除済み。

---

# Phase 1 — D0ピーク(〜2週)

## Issue #5: 処方箋リビール画面(D0感情ピーク)を実装 [M] ★このフェーズの本丸

**背景**: オンボ5ステップは全て設定作業で、感情の山なしにペイウォールが出る(平地で売っている)。世界の勝ちパターン(I am型)はD0の山の直後に売る。

**実装設計**:
- 新規 `PrescriptionRevealView.swift`。OnboardingView の `showPremiumAtEnd = true` の前に挟む(`showPrescription` 状態を追加し、リビール完了後にPremiumViewへ)
- 画面フロー(3拍):
  1. **予感(1.5〜2.0秒)**: 「あなたの答えから、言葉を選んでいます」+ 選択済みタグ(focus/topics/tone)がゆっくり収束するアニメーション。ここで世界観をライト→ダークへトランジション(#監査P2「世界観の分裂」をここで物語として接続)
  2. **開示**: 「あなたの処方箋」カードをリビール。内容 = ①今のあなた(選択の要約を2文で言語化) ②最初の一枚(選択に最も合うOriginal or 高品質名言を1枚、実物を見せる) ③これから届くもの(朝/夜の受け取り方)
  3. **着地**: 「この処方箋を、7日間ぜんぶ受け取れます」→ ペイウォールへ(#6の贈与フレームに接続)
- 処方文の生成はテンプレ+選択の組み合わせ(LLM不要)。`WordTone × QuoteLargeCategory` で9パターンの要約文を用意
- 図星の一線を守る: 行動の再現はよいが動機の名指し(「見捨てられ不安ですね」等)は禁止

**文言案(gentle × selfGrowth の例)**:
> 「がんばり方は知っているのに、休み方を自分に許していない。
> あなたに必要なのは、背中を押す言葉より、肩の力を抜く言葉。」

**計測**: `logPrescriptionView()` 新設(AnalyticsService)。ファネル: onboarding_complete → prescription_view → paywall_view(trigger: "onboarding_prescription")

**受け入れ条件**:
- [ ] 予感の窓が1.5秒以上ある(即時表示しない)
- [ ] 処方箋に「実物の言葉1枚」が含まれる(約束だけで終わらない)
- [ ] リビール後にのみペイウォールが出る
- [ ] スキップ導線がある(審査対策+強制感の除去)

## Issue #6: トライアルを贈与フレーミングに変更 [S] (依存: #5)

**背景**: 現CTAは「◯日無料で始める」の標準文言。贈与(返報性)として渡すと転換とその後の関係が変わる。

**実装設計**: PremiumView の文言のみ変更(ロジック不変)。イントロオファー適格時:
- ヘッドライン: 「処方箋が決まった記念に」
- サブ: 「7日間、すべての言葉と棚を受け取れます」
- CTA: 「7日間、受け取ってみる」(introOfferCTA を差し替え)
- 補足(既存checkoutSupportingTextを流用): 「◯日の無料期間終了後、¥4,800/年で自動更新されます。終了前に通知します」→ **終了前日ローカル通知を実装**(NotificationService に trialEndDate - 1日 のスケジュール追加。UserSettings.trialEndDate は既存)
- 非適格ユーザー(トライアル済み)には贈与文言を出さない(嘘になる)

**受け入れ条件**:
- [ ] 「トライアル」「無料お試し」という語がユーザー向け文言から消えている
- [ ] 終了前日通知が実際に飛ぶ(実機確認)
- [ ] 適格/非適格で文言が正しく出し分けられる

## Issue #7: ペイウォール特典の文脈別並び替え [S]

**実装設計**:
- `PremiumView(context: PaywallContext)` を追加。`enum PaywallContext { case onboarding, favoriteLimit, archive, wallpaper, categoryLock, insight, general }`
- 既存の呼び出し7箇所(ContentView×3, FavoritesView, ArchiveView, SettingsView, OnboardingView)に context を渡す(logPaywallView の trigger 文字列と対応させる)
- `benefitListSection` を4項目に削減し、context に応じ先頭を入れ替え:
  - favoriteLimit → 「言葉の棚を無制限に」が先頭
  - archive → 「過ぎた言葉をさかのぼる」
  - categoryLock/onboarding → 「全カテゴリを自由に」
  - wallpaper → 「背景と空気感を自分好みに」
- 「通知3回」は特典リストから比較表へ格下げ(訴求力が弱い)

**受け入れ条件**:
- [ ] 全呼び出し箇所で context が渡っている
- [ ] 特典が4項目以内
- [ ] trigger別の転換率が計測で分離できる(logPurchaseInitiate の trigger に context を反映)

## Issue #8: ファネルKPI計測の整備 [S]

**正式運用仕様**: `QuoteApp/docs/experiment-operations-v2.md` の「KPI読み取り契約」を正とする。分子・分母・対象期間・版を同じカードで表示し、D7未到達コホートを分母へ入れない。

**背景**: イベントは既に豊富(logTrialStart, logPaywallView等)。欠けているのは「ファネルとして読める形」。

**実装設計**:
- 不足イベント追加: `logPrescriptionView`(#5)、`logTrialConvert`(RevenueCat webhookまたは起動時のsubscriptionStatusCheckで検出)
- Firebase(または現行の分析基盤)で目標ファネルを定義:
  `first_open → onboarding_complete → prescription_view → paywall_view(onboarding) → trial_start → trial_convert`
- 併せて北極星系: D1/D7残存、favorite_add/日、share_complete/週
- 週次で見る数値を1画面に: 監査ロードマップのゲート(トライアル開始率8%/転換25%)と紐づけ
- ダッシュボードが作れない場合の代替: 週次でFirebaseコンソールのファネルをスクショ保存する運用手順を README化

**受け入れ条件**:
- [ ] トライアル開始率・転換率・D7が「割り算せずに」読める状態
- [ ] ゲート値(8% / 25%)が判定できる粒度でイベントが揃っている

## Issue #20: 誤帰属・出典不明15件の整理とOriginal置換 [M]

**背景**: 監査B節。Einstein 4件は有名な偽名言。信頼が一度崩れると全訴求が無効化する。

**実装設計**:
1. **削除対象(確度高)**: 108, 109, 122, 136, 186(Einstein) / 137, 187(Jobs) / 185, 188(T.Roosevelt) / 127, 141(出典なし) / 121, 123(旧・朝カテゴリ残骸)
2. **修正対象**: 140(Mother Teresa)は「宇宙」→原文通り「神」に戻すか削除(改変引用は不可)
3. 残す判断をしたものは source_context を「〜の言葉として広く引用される(一次出典は確認されていない)」に統一
4. 削除枠(13〜15件)は #22 のOriginalで補充。カテゴリ配分を維持(želse各カテゴリ20件から凹んだ分)
5. 検証スクリプトを `scripts/validate_quotes.py` として恒久化: kana整合・重複・EN/JA対応・カテゴリ件数を CI的にチェック

**受け入れ条件**:
- [ ] 有名偽名言(insanity等)が消えている
- [ ] 改変引用が0件
- [ ] validate_quotes.py が全チェックpass

## Issue #21: 分類ミス10件の再配置とカテゴリ設計修正 [M]

**背景**: 監査D節。個別ミスより、love_crush と want_to_quit の設計ズレが本丸。

**実装設計**:
1. **個別再配置**: 085→inner_strength / 091→self_love / 145→courage / 159, 134, 170, 173, 175, 181→削除候補(どこにも合わない) / 感謝系5件(101,102,107,143,174)→「小さな感謝」テーマとしてpositive集約 or 削除
2. **love_crush 再定義**: 「愛の哲学」(Gibran/Fromm/Rilke等の普遍論)は relationships へ移動。love_crush は「恋の渦中の揺れ」専用に: 連絡待ち/比較/不安/自分を見失う/片思い。不足分は #22 で補充
3. **want_to_quit トーン転換**: 叱咤系(「諦めない姿勢が〜」「困難と戦う姿は美しい」)を除外し、免罪系(「時には、生きることそのものが勇気ある行為だ」063 が基準)で統一。**このカテゴリの言葉は全件「限界の日の夜に読める」ことを合格基準にする**
4. オンボの説明文言(mediumSubtitle)と中身の一致を最終確認(「好きな人との距離感や気持ちを見つめたい」に応える中身か)

**受け入れ条件**:
- [ ] 全300件を displayTitleJa の定義文と突き合わせて違和感リスト0
- [ ] want_to_quit に命令形・叱咤の言葉が0件
- [ ] love_crush の8割以上が「恋の渦中」の言葉

---

# Phase 2 — 儀式(3〜5週)

## Issue #9: 夜の言葉(朝/夜トーン切替+免罪トーン) [L] ★このフェーズの本丸

**背景**: 21:30通知は選べるが中身が朝と同じ。「許されたい」が走る時間帯に免罪の儀式を作る(統合方針フェーズ3)。**この文脈では売らない。**

**実装設計**:
- **コンテンツ**: 夜専用プール `night_words` を quotes.json に追加(または category とは別の `timeContext: "night"` フィールド)。第一弾は #22 のOriginal 20件(免罪トーン)
- **UI**: 21時以降の起動 or 夜通知からの起動で、通常スワイプ画面の前に「夜の一枚」を全画面表示(既存のMainQuoteViewを流用、背景は quietNight 系固定)。一枚だけ。スワイプ台に混ぜない(儀式は消費と分ける)
- **一言残す**: 夜の一枚の下に「今日をひとことで置いていく」入力(任意・1行)。既存 ReflectionComposer の軽量版。保存先は favoriteNote と同じ仕組みで日付に紐づけ(ArchiveViewで見返せる)
- **通知**: ReminderPreference.night 選択者の21:30通知を夜プールから配信。文言は #10 の予感型を先行適用
- **課金導線**: この画面には一切置かない(倫理ライン)。プレミアム訴求もバッジも出さない

**文言案(夜のOriginal例、#22と共通)**:
> 「今日をここまで運んだだけで、じゅうぶんです。」
> 「返せなかった連絡は、明日のあなたに任せていい。」
> 「うまく言えなかった夜は、言葉にしなくていい夜でもある。」

**計測**: `logNightWordView`, `logNightNoteSave`。夜儀式利用者のD7/D30を非利用者と比較(このIssueの成否判定)

**受け入れ条件**:
- [ ] 21時以降の初回起動で夜の一枚が出る(2回目以降の起動では出ない)
- [ ] 夜の画面に課金導線が存在しない
- [ ] 一言がアーカイブから日付で見返せる
- [ ] 夜プールの言葉に叱咤・命令形が0件

## Issue #10: 通知文言の予感型A/Bテスト [S] (依存: #9)

**正式運用仕様**: `QuoteApp/docs/experiment-operations-v2.md` の「E10 通知文言A/B」を正とする。開始日、最低サンプル、延長条件、判定式、敗者削除までを定義済み。

**背景**: 現在 `content.body = quote.quoteJa`(答えを通知に書いている)。ただし名言アプリでは「通知だけで完結する価値」も実在するため、**全面変更ではなくA/Bで判定**。

**実装設計**:
- NotificationService に文言モード(full / tease)を追加。Remote Config or ローカル50%割当(userIdハッシュ)
- tease型の文言案(夜): 「今日のあなたに、置いていきたい言葉があります」/ (朝): 「今朝の一枚を選びました。◯◯さんの言葉です」(著者名まで見せて中身は見せない)
- 判定指標: 通知開封率(logNotificationOpen 既存)、開封後のfavorite_add率、7日後の通知オフ率。**開封率が上がっても通知オフが増えたらtease負け**
- 2週間で判定し、負けた方を削除

**受け入れ条件**:
- [ ] 割当が安定している(同一ユーザーは常に同じ群)
- [ ] 判定3指標がイベントで取れる
- [ ] 判定期限(開始から2週間)がタスク化されている

## Issue #11: ストリーク+記録保護(救済型) [M]

**背景**: **UserSettings に currentStreakDays / longestStreakDays / lastEngagementDate が既に存在する**(計測済み・未表示と推定)。UIと保護の仕組みだけが無い。

**実装設計**:
- 既存フィールドの更新ロジックを確認し、「1日1回言葉を受け取る(表示)」でカウントされることを保証
- **表示**: メイン画面の日付ヘッダー付近に控えめに「7日目」程度(炎アイコン等の圧は出さない。世界観に合わせ「今日で7日、言葉を受け取っています」)
- **保護(サブスク特典)**: 切れた**翌日**に「昨日までの14日は消えていません。今日の言葉から続きにできます」→ 復元はプレミアム特典。切れる**前**に脅す通知は実装しない(倫理ライン: 救済であって脅迫ではない)
- 無料ユーザーの切れた記録は longestStreakDays として保存し続ける(失われない事実は正直に)
- 休息設計: 週1回まで「おやすみ日」が自動適用(1日空いてもストリーク継続)。積み上げ強要による疲労離脱の防止

**計測**: `logStreakBroken(days:)`, `logStreakRestore(days:)`。保護経由のpaywall_view trigger追加

**受け入れ条件**:
- [ ] 切れる前に一切通知・警告が出ない
- [ ] 切れた翌日の案内から復元までが動く
- [ ] おやすみ日が機能する(23時台→翌日深夜1時の連続利用でも切れない等、境界時刻のテスト)

---

# Phase 3 — 積み上げ(6〜9週)

## Issue #12: 週次「私の棚」サマリー [M]

**背景**: 棚はあるが「並びから自分が見えてくる」報酬が無い。サブスクの継続理由の柱。

**実装設計**:
- 日曜21時のローカル通知「今週のあなたの棚が整いました」→ 専用画面 `WeeklyShelfView`
- 内容: ①今週保存した言葉(枚数と一覧) ②テーマの重心「今週は『回復』の言葉を集めています」(categoryAffinityScores の週次差分から算出、LLM不要) ③今週の一枚(最も長く見た/ノートを書いた言葉)
- **無料ユーザー**: 表紙(枚数+テーマ名)まで。中身はぼかし+「棚の中は、プレミアムで開けます」→ PaywallContext.weeklyShelf 新設
- 保存0件の週は出さない(空のサマリーは逆効果)。代わりに月1回まで「今週は棚に置く言葉がありませんでした。それもそういう週です」型の免罪通知
- シェア: ③今週の一枚を既存 getShelfShareItems 形式で書き出し(#16と接続)

**受け入れ条件**:
- [ ] 保存者だけに日曜夜に届く
- [ ] テーマ算出が実データで妥当(手元データでのスポットチェック)
- [ ] 無料/有料の見え方の差が実装通り

## Issue #13: 月次言葉レポート+月替わりパック [L] (依存: #12)

**背景**: サブスクが「毎月生む価値」が現状ゼロ。解約防止の構造を作る。app-monetization.md の原則: 静的読み放題は読み終わった月に解約される。

**実装設計**:
- **月次レポート**(プレミアム限定): 月初に先月分を生成。①保存数と棚の成長 ②テーマの変化(先月→今月の重心移動を1文で: 「『踏ん張る』から『ゆるめる』へ動いた月でした」テンプレ生成) ③今月のベスト3(自分の保存から) ④来月の処方箋(オンボ処方箋の月次更新版)
- **月替わりパック**: 月初に新規言葉20件(Original中心)+限定背景1種を追加、**月末に本当にローテアウト**(翌月は別内容。保存済みの言葉は棚に残る=損失回避と誠実さの両立)。実装済みの SpiritualBundle / DailyRitualState / TodayWordSelection 資産を月替わりの器として再活用検討
- 通知は月初1回のみ「今月の言葉が届きました」

**受け入れ条件**:
- [ ] レポートが実データから自動生成される
- [ ] 月末ローテアウトが実際に発生し、保存済みは棚に残る
- [ ] 「消える」と書いたものは本当に消える(偽限定ゼロの検証)

## Issue #23: 解説文テンプレの構文多様化 [M]

**背景**: preview の90%が同一構文、81件は「勇気がなければ、」は〜 の読点切り。プレミアム体感品質に直結。

**実装設計**:
1. **最優先(機械修正可)**: 読点切り81件 — 引用部を「意味の切れ目まで」延長 or 引用をやめて要旨で書き出す。スクリプトで対象抽出→一括書き換え→目視確認
2. 構文バリエーションを5型に分散(現1型):
   - 要旨先行型「怖さを消すのではなく、怖いまま進める形に整える。◯◯はそういう言葉です」
   - 場面先行型「うまくいかない日の夜に効くのは、この言葉のこの部分です」
   - 著者先行型「◯◯がこれを語ったのは〜の場面でした」
   - 問いかけ型 / 現行型(2割まで残す)
3. premium の3ステム循環も同様に分散。stop-ai-slop-jp基準(リズムの均一化・翻訳調・偏愛語)で全件パス
4. 書き換えは20件/バッチで実施し、各バッチ音読チェック

**受け入れ条件**:
- [ ] 読点切り鉤括弧が0件
- [ ] 同一書き出しステムが全体の30%以下
- [ ] サンプル20件の音読チェックで機械感の指摘0

## Issue #22: Original創作短文を20→60件へ増強 [L] (依存: #20, #21)

**背景**: 削除枠(重複+誤帰属+権利リスク≒35〜40件)を全てOriginalで埋める。権利ゼロ+差別化の核(日本語ネイティブ品質)。

**実装設計**:
- 配分案(+40件): want_to_quit免罪トーン +12 / love_crush恋の渦中 +12 / 夜の言葉用(#9) +10 / 感謝・positive系の欠員補充 +6
- 品質基準 = 既存Original20件 + 統合方針の創作短文条件(強く断定しない/やさしいが弱すぎない/読んだ瞬間に気分の温度が少し変わる/受け手の余白を残す)
- 制作プロセス: ①カテゴリごとに「場面リスト」を先に作る(例: love_crush = 既読無視の夜/インスタで元恋人を見た/друг友達の結婚報告) ②場面→言葉を起こす ③stop-ai-slop-jpチェック(図星→免罪の順、動機の名指し禁止) ④10件ごとに音読
- author は「Original」を維持、author_description は「Words For Me」等ブランド名に統一するか検討(現状の空author_factは埋める必要なし)
- meaning_preview/premium も同時制作(#23の新構文で書く)

**文言サンプル(love_crush・恋の渦中)**:
> 「返事が来ない時間は、あなたの価値が測られている時間ではない。」
> 「好きな人の前で小さくなる癖は、恋のせいで、あなたのせいじゃない。」

**受け入れ条件**:
- [ ] 総数300件、各カテゴリ30件、Original 60件以上の配分を維持
- [ ] validate_quotes.py 全pass
- [ ] 全新規件がstop-ai-slop-jpチェック済み(偏愛語・翻訳調・説教トーン0)

---

# Phase 4 — 第二の財布と獲得(10〜13週)

## Issue #14: 言葉のお守りギフト(消費型IAP) [L]

**正式仕様**: `QuoteApp/docs/gift-omamori-product-spec.md` を正とする。反復購入、API、購入検証、冪等発行、再送、365日失効、返金、プライバシー、受取体験を定義済み。

**背景**: 第二の財布+バイラル導線。自動更新サブスクはアプリ内で他人に贈れないため非更新型で実装。

**実装設計**:
- 商品: 非消耗型 or 非更新サブスク `com.quoteapp.gift.omamori`(¥480)。内容: 選んだ言葉1枚+贈り主の一言+特別背景を「お守りカード」として相手に届ける
- 配送: 受け取りリンク(Universal Link)方式。相手はアプリ無しでもWebで開ける(獲得導線)→ アプリで開くと保存できる
- 贈る側のフロー: 棚 or 共有画面から「お守りにして贈る」→ 一言添える → 購入 → リンク共有(LINE前提)
- 受け取り通知文はスクショされる前提で: 「◯◯さんから、言葉のお守りが届いています」
- ASC設定は iap-config-sync スキルの管理下に追加
- 倫理: 受け取り側に課金要素を見せない(お守りを開く体験は完全無料)

**受け入れ条件**:
- [ ] 贈る→受け取る→保存が2台の実機で通る
- [ ] 受け取り側フローに課金導線が無い
- [ ] logGiftPurchase / logGiftOpen 計測

## Issue #15: 変動報酬型の深掘り提案(3回に1回) [S]

**実装設計**:
- 現在: 保存後は毎回同じ流れ(ReflectionComposer)。変更: 保存後の追加提案(「この言葉の続きを読む」= QuoteInsight誘導)を**確率1/3**で表示(quoteIdハッシュで決定論的に、同じ言葉では常に同じ挙動=不具合に見えない)
- 基本機能(保存・内省)は常に固定。揺らすのは追加の喜びだけ
- 提案の文言も2種ローテ: 「この言葉には、もう一段あります」/「◯◯がこれを言った時の話、読みますか」

**受け入れ条件**:
- [ ] 保存フロー自体は毎回同じ(基本機能が不安定に見えない)
- [ ] 提案表示率が実測で30〜36%に収まる
- [ ] 提案経由のinsight開封率を毎回表示していた頃と比較できる

## Issue #16: UGCシェア強化+TikTok/Reels運用開始 [L]

**正式運用仕様**: コピーと最初の12本は `QuoteApp/docs/copy-strategy-v2.md` 2-7、4週間の投稿計画・キャンペーンリンク・継続/破棄基準は `QuoteApp/docs/experiment-operations-v2.md` E16を正とする。

**背景**: I am型の獲得エンジン。**getShelfShareItems(棚シェア)は既に実装済み** — フォーマット追加と運用開始が本体。

**実装設計**(アプリ側):
- シェアフォーマット追加(統合方針§8): 「今週の私の3枚」(週次サマリー#12から1タップ)/「夜に残したい一枚」(夜の言葉#9から)/ 既存Stories形式の背景に月替わり限定背景を使用可に
- 画像の下部ラベルを「KOTODAMA EDIT」から「Words For Me」ロゴ+App Store誘導の小さな導線に(主張しすぎない、統合方針の見栄え条件維持)
- シェア画像に含める文言はユーザーの選択(言葉のみ/言葉+自分の一言)

**実装設計**(運用側・アプリ外):
- TikTok/Reels アカウント開設。週3投稿×4週の実験: ①今日の言葉(画像+環境音) ②「◯◯な夜に効く言葉3選」型リスト ③処方箋診断の切り出し(オンボの5問を縦動画化)
- 計測: プロフィールリンク経由インストール(App Store Connect のキャンペーンリンク)。4週でフォロワー/保存率/インストールを見て型を絞る

**受け入れ条件**:
- [ ] 新フォーマット2種がアプリから1タップで書き出せる
- [ ] キャンペーンリンクでインストール計測が分離できる
- [ ] 4週間の投稿実験が完了し、続ける型/捨てる型が決まっている

---

# 並行 — 土台(全期間)

## Issue #17: 全画面トークン置換+アクセシビリティ [L] (依存: #3)

**実装設計**:
- 置換順: PremiumView(売上直結) → OnboardingView → MainView → Favorites/Archive/Settings/CategoryPicker → Widget
- 各画面の置換PRで同時に: ①固定フォント→relativeTo付き ②アイコンのみボタンに accessibilityLabel ③ReduceMotion時のアニメ簡略化(`@Environment(\.accessibilityReduceMotion)`) ④角丸をRadiusスケールへ
- 検証: 画面ごとにライト/ダーク+Dynamic Type最大(AX5)のスクショをビフォーアフターで保存(ios-simulator-automation使用)
- 「見た目を変えない」が原則。変える場合(角丸統一で1-2pt動く等)はスクショ差分で明示

**受け入れ条件**:
- [ ] scan.sh 再実行でハードコード色・固定フォントのヒットが90%減
- [ ] AX5でレイアウト破綻が無い(主要5画面)
- [ ] VoiceOverで主要フロー(オンボ→今日の言葉→保存)が完走できる

---

# 再設計 v2 — 「儀式の一枚」(2026-07-11 GO確定)

戦略再定義: **SNSは言葉に出会う場所、Words For Me は言葉が私のものになる場所**(詳細: [コピー戦略_SNS非対称戦_2026-07-11.md](コピー戦略_SNS非対称戦_2026-07-11.md))。
体験は二層構造に組み直す: **儀式層(1日一枚を「引く」)が起動の顔、選書層(現行スワイプデッキ)が深掘り**。スワイプと課金装置は無傷で維持する。

## Issue #24: 起動体験の二層化「儀式の一枚」 [M] ★v2の本丸 (依存: #5 / これが #9 をブロック)

**正式仕様**: `QuoteApp/docs/daily-draw-v2-spec.md` を正とする。既存 `DailyRitualView` は任意の深い儀式へ退避し、自動起動は `DailyDrawView` に一本化する。独立した夜画面は21時以降の選定分岐として統合する。

**背景**: 起動=いきなりスワイプ台で、「毎日の言葉の儀式」という戦略と実装が逆向き。スワイプは選書の所作(予感ゼロ・摩耗速い)、儀式には「引く」の所作が要る。TodayQuoteView は実装済みだが未接続の孤児コード。「引く」はSNSが構造的に再現できない器であり、占い財布(997億円・顧客90%女性)への非占い接続点。

**実装設計**:
- 新規 `DailyDrawView.swift`(孤児の TodayQuoteView を転用改修 or 置換。#5 処方箋リビールの予感→開示の演出部品を共通コンポーネント化して共有)
- `UserSettings.lastDrawDate: String`(yyyy-MM-dd)を追加。ContentView 表示時に `lastDrawDate != today` なら DailyDrawView を最前面に表示
- 所作の流れ: 伏せたカード(背面=選択済み atmosphere の背景)→タップで引く(ユーザー主導=予感の窓)→めくり演出1.5秒+ハプティクス→開示→CTA「棚に置く」/「今日はこれだけ」/「もう少し読む」→スワイプデッキへ
- 選定ロジック: 既存 `getDailyQuotes` の affinityScores を流用。基本は preferredCategories から、**3回に1回だけ affinity 最上位のドンピシャ枠**(変動報酬。毎回ドンピシャにしない)
- 時間帯分岐: hour >= 21 → 夜プール(#9 と接続)。それ以外は通常プール
- **同日2回目以降の起動は直接デッキ**(I am 的な摩擦ゼロを殺さない)。通知・ウィジェット起動でも当日未ドローなら儀式から
- 無料/有料で儀式は同一体験。**儀式層に課金導線・ロック・バッジを置かない**(売るのはデッキ側のまま)
- 引かない自由: 「今日は引かない」導線あり(スキップでデッキへ。強制は儀式を義務に変える)

**文言**(コピー戦略 2-1/2-6 準拠): 導入「今日のあなたに、一枚。」/ 夜「今日を、許して閉じる。」

**計測**: `logDailyDraw(timeSlot:)` `logDrawSave` `logDrawToDeck` `logDrawSkip`。判定指標: 儀式実行者 vs 非実行者の D7/D30。

**受け入れ条件**:
- [ ] 1日1回だけ表示される(日付境界・タイムゾーン・23:59→0:01 のテスト)
- [ ] 同日2回目以降の起動では挟まらない
- [ ] 儀式層に課金導線が存在しない
- [ ] めくる前にユーザーのタップを待つ(自動でめくらない=予感はユーザーの指で作る)
- [ ] TodayQuoteView の孤児コードが転用または削除で整理されている
- [ ] デッキ側の課金装置(11枚目ロックプレビュー・枚数制限)が無傷

## Issue #25: ASO・ストア表現の全面更新 [S] (依存: #24 と同時リリース)

**正式コピー**: `QuoteApp/docs/copy-strategy-v2.md` 2-2を正とする。ローカルの `fastlane/metadata/ja/` は同コピーへ更新済み。スクリーンショットは#24実装後に実画面だけで再生成する。

**背景**: 「引いて、棚に置く」が実装されるタイミングで、ストア表現を SNS非対称戦ポジショニングへ一斉更新。

**実装設計**(コピー戦略 2-2 の実物を反映):
- タイトル: `Words For Me - 今日の私に効く言葉・名言` / サブタイトル: `一枚引いて、棚に置く。夜のことばのお守り`
- 説明文冒頭3行を「保存墓場」フックに差し替え / キーワード欄を建前語で更新
- スクショ見出し4枚: 「保存で終わらせない。」「今日の私に、一枚。」「夜は、許される言葉を。」「棚を見れば、私がわかる。」(作成は appstore-screenshot-jp-conversion / 反映は appstoreconnect-metadata-sync)
- スクショに架空要素(存在しないUI・偽レビュー)を含めない

**受け入れ条件**:
- [ ] メタデータがコピー戦略docと一致
- [ ] スクショが実装済みUIのみで構成されている

## 既存Issueへの差分(v2組み直し)

| Issue | v2での変更 |
| :-- | :-- |
| #5 処方箋リビール | 位置づけ=「初回の引く」。予感→開示の演出を**#24と共有する共通部品として実装**。コピーは戦略doc 2-3で確定 |
| #9 夜の言葉 | **#24の夜側**として実装(独立画面ではなく二層構造のゲートに乗る)。依存: #24 |
| #10 通知A/B | B群(tease)文言を戦略doc 2-5で確定。「引く」体験の予告として機能 |
| #12 週次サマリー | リフレーミング: 「言葉が還ってくる」体験。保存墓場(SNS)との対比が訴求の核 |
| #13 月替わりパック | 「引く」の月次拡張(毎月新しい山を引ける)。SpiritualBundle資産をカード背面・所作の演出に転用 |
| #16 UGC運用 | 最初の12本の台本=戦略doc 2-7のフック6型。CTA固定「流されない場所に、置いておく。」 |
| #2 #6 ペイウォール | コピーを戦略doc 2-4で確定(「流れて消える言葉と、残って効いてくる言葉は、別のものです」) |
| #22 Original増強 | #19の削除3件(相田みつを2+書名1)の補充を配分に追加(+3)。夜プール用を最優先で執筆 |
| #19 権利判断 | **完了**(2026-07-11 決定記録参照: 3件削除済み・存命人物は継続方針を文書化) |

# 実施順序 v2(依存関係込み)

```
週1        : #1 #2 #4 #18 (#19✓完了) → データ修正版リリース
週1-2      : #3 → #17(Premium先行) / #5(初回の引く) → #6 #7 #8
週2-3      : #24(毎日の引く=二層化) / #20 #21 → ★「儀式の一枚」版リリース(勝負リリース)
週3-5      : #9(夜の引く) #11(ストリーク) / #22(Original執筆は週1から並行) → #10
週6-9      : #12(還ってくる棚) → #13 / #23 / #17(残り) / #25(ASO更新、#24と同時が理想)
週10-13    : #14 #15 #16(保存墓場攻撃の運用開始)
```

ゲート判定(v2): 「儀式の一枚」版リリース後2週で、①D0トライアル開始率8%(#8ファネル) ②儀式実行者のD7が非実行者を上回るか(#24計測)の2つを判定。①未達→#5/#24のコピー・演出改善で再測。②で差が出ない→儀式の摩擦を疑う(表示頻度・所作の長さ)。
